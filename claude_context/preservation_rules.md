# AIRISS 절대 보존 규칙 (CRITICAL PRESERVATION RULES)

## 🚨 절대 변경 금지 항목 (NEVER TOUCH)

### 📡 기존 API 엔드포인트
```
❌ 절대 변경 금지:
- /api/analyze/{file_id}
- /api/upload  
- /api/download/{file_id}
- /api/files
- /api/health
- /websocket
- /dashboard (메인 대시보드)
- / (루트 페이지)

✅ 새 기능은 별도 경로 사용:
- /api/v2/* (v5 신기능용)
- /api/experimental/* (실험 기능용)
- /api/admin/* (관리 기능용)
```

### 🗄️ 데이터베이스 스키마
```sql
-- 절대 변경 금지 테이블들
❌ uploaded_files (id, filename, upload_time, file_path, status)
❌ analysis_results (id, file_id, uid, overall_score, grade, confidence, ...)
❌ 기존 컬럼 타입/이름 변경 금지

✅ 허용되는 변경:
- 새 테이블 추가 (v5_*, experimental_* 등)
- 기존 테이블에 새 컬럼 추가 (기본값 설정 필수)
- 인덱스 추가 (성능 개선용)
```

### 🏗️ 핵심 파일 구조
```
❌ 절대 변경 금지:
- main.py (기존 라우터 등록 부분)
- app/services/hybrid_analyzer.py (핵심 분석 로직)
- app/services/text_analyzer.py (텍스트 분석 프레임워크)
- app/services/quantitative_analyzer.py (정량 분석)
- app/db/sqlite_service.py (데이터베이스 기본 연결)
- app/templates/dashboard.html (기존 UI 구조)

✅ 수정 허용 범위:
- 새 파일 추가 (app/services/v5_*.py)
- 기존 파일에 새 함수 추가 (기존 함수 수정 금지)
- CSS/JS 추가 (기존 스타일 덮어쓰기 금지)
```

---

## ⚙️ 기능 보존 규칙

### 📊 기존 분석 워크플로우
```
1. 파일 업로드 → 2. 데이터 추출 → 3. 하이브리드 분석 → 4. 결과 표시 → 5. 엑셀 다운로드

❌ 이 플로우의 어떤 단계도 변경/제거 금지
✅ 플로우에 새 단계 추가 가능 (선택적)
✅ 기존 단계의 성능 최적화 가능
```

### 🎯 8대 차원 분석 시스템
```
절대 보존해야 할 차원들:
1. 업무성과 (Work Performance)
2. 리더십협업 (Leadership & Collaboration)  
3. 커뮤니케이션 (Communication)
4. 전문성학습 (Expertise & Learning)
5. 태도열정 (Attitude & Passion)
6. 혁신창의 (Innovation & Creativity)
7. 고객지향 (Customer Orientation)
8. 윤리준수 (Ethics & Compliance)

❌ 차원 이름 변경 금지
❌ 차원 삭제 금지  
❌ 기존 점수 계산 로직 변경 금지
✅ 새 차원 추가 가능 (9번째 차원 등)
✅ 각 차원의 정확도 개선 가능
```

### 📈 시각화 및 차트
```
보존해야 할 Chart.js 시각화:
- 8대 차원 레이더 차트
- 점수 분포 히스토그램  
- 실시간 분석 진행률 차트
- 파일별 결과 비교 차트

❌ 기존 차트 제거 금지
❌ 차트 데이터 구조 변경 금지
✅ 새 차트 추가 가능
✅ 차트 스타일 개선 가능
```

---

## 🔒 성능 기준 보존

### ⏱️ 응답 시간 기준
```
절대 지켜야 할 성능 기준:
- 파일 업로드: < 3초
- 텍스트 분석: < 5초 (레코드당)
- 결과 조회: < 1초
- 대시보드 로딩: < 2초
- 엑셀 다운로드: < 10초

❌ 기존 기능의 성능 저하 불허
✅ 새 기능의 성능은 별도 관리
✅ 전체적인 성능 향상은 환영
```

### 💾 메모리 사용량
```
최대 허용 메모리:
- 기본 운영: 2GB 이하
- 대량 분석 시: 4GB 이하
- 동시 접속자 50명 기준

❌ 메모리 누수 발생시키는 코드 금지
❌ 무제한 메모리 증가 코드 금지
✅ 메모리 효율성 개선 권장
```

---

## 🛡️ 보안 및 데이터 보호

### 🔐 기존 보안 설정
```
절대 변경 금지:
- 현재 인증/인가 체계
- 파일 업로드 제한 (크기, 형식)
- 데이터 암호화 방식
- CORS 설정
- 환경변수 구조

❌ 보안 수준 낮추는 변경 금지
✅ 추가 보안 강화는 권장
✅ 새 보안 기능 추가 가능
```

### 📁 파일 시스템 보호
```
보존해야 할 디렉토리 구조:
/uploads/ - 업로드 파일 저장
/static/ - 정적 파일 
/templates/ - HTML 템플릿
/app/services/ - 핵심 서비스 로직

❌ 기존 파일 이동/삭제 금지
❌ 권한 설정 변경 금지
✅ 새 디렉토리 추가 가능
```

---

## 🔄 호환성 보존

### 📱 브라우저 호환성
```
지원해야 할 브라우저:
- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

❌ 기존 지원 브라우저에서 동작 안되게 하는 변경 금지
✅ 새 브라우저 지원 추가 가능
✅ 모바일 호환성 개선 가능
```

### 🔌 외부 연동 시스템
```
기존 연동 시스템:
- Chart.js 라이브러리
- SQLite 데이터베이스
- FastAPI 프레임워크
- WebSocket 통신

❌ 기존 라이브러리 버전 변경시 호환성 검증 필수
❌ 외부 API 의존성 제거 금지
✅ 새 라이브러리 추가 가능 (기존과 충돌 없는 경우)
```

---

## 🚨 위험 상황 대응

### 🆘 긴급 롤백 조건
```
즉시 롤백해야 하는 상황:
1. 기존 기능이 동작하지 않음
2. 성능이 기준치 이하로 떨어짐
3. 데이터 손실이나 손상 발생
4. 보안 취약점 발생
5. 사용자 접근 불가 상황

롤백 절차:
1. 백업 파일로 즉시 복구
2. 서비스 재시작
3. 기능 검증
4. 원인 분석 후 재시도
```

### 📋 변경 전 체크리스트
```
모든 변경 전 반드시 확인:
□ 기존 기능 테스트 통과
□ 성능 기준 만족
□ 보안 검증 완료
□ 백업 생성 완료
□ 롤백 계획 준비
□ 영향도 분석 완료
```

---

## 💡 권장 사항

### ✅ 안전한 개발 방식
```
1. 기능 분리: 새 기능은 별도 모듈로 개발
2. 점진적 통합: 단계별로 조금씩 통합
3. 독립적 테스트: 새 기능만 따로 테스트 후 전체 테스트
4. 선택적 활성화: 기능 플래그로 on/off 가능하게
5. 성능 모니터링: 변경 후 지속적 성능 확인
```

### 🎯 개발 우선순위
```
1순위: 기존 시스템 안정성 보장
2순위: 새 기능의 독립성 확보  
3순위: 성능 최적화
4순위: 사용자 경험 개선
5순위: 추가 기능 개발
```

---

## 📞 문제 발생시 대응

### 🚨 에스컬레이션 절차
```
Level 1: 일반 오류 (개발자 처리)
Level 2: 기능 장애 (팀장 보고)
Level 3: 서비스 중단 (CTO 보고)
Level 4: 데이터 손실 (CEO 보고)

연락처:
- 개발팀: [개발팀 연락처]
- 인프라팀: [인프라팀 연락처]  
- 긴급상황: [긴급 연락처]
```

### 📝 변경 이력 관리
```
모든 변경사항은 반드시 기록:
- 변경 일시
- 변경 내용
- 변경 사유
- 영향 범위
- 테스트 결과
- 담당자 정보
```

---

**"안정성이 혁신의 기반입니다. 기존 시스템을 보호하면서 발전시켜 나갑시다!"** 🛡️