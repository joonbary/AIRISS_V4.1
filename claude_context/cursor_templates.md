# 커서AI용 프롬프트 템플릿 모음

## 🎯 기본 템플릿 구조

### 📋 표준 프롬프트 템플릿
```markdown
# AIRISS 프로젝트 작업 지시서

## 📋 프로젝트 컨텍스트
- **프로젝트명**: AIRISS v4.1 → v5.0 AI 기반 인재 스코어링 시스템
- **기술스택**: FastAPI + SQLite + HTML/JS + Chart.js + WebSocket
- **현재상태**: v4.1 안정 운영 중 (https://web-production-4066.up.railway.app/)
- **핵심기능**: 8대 차원 텍스트+정량 하이브리드 분석
- **작업폴더**: C:\Users\apro\OneDrive\Desktop\AIRISS\airiss_v4

## 🎯 작업 목표
[구체적인 목표를 여기에 명시]

## ⚠️ 절대 보존 규칙 (CRITICAL)
✅ **절대 변경 금지:**
- 기존 API 엔드포인트 (/api/analyze, /api/upload, /api/download 등)
- 데이터베이스 스키마 (analysis_results, uploaded_files 테이블)
- main.py의 기존 라우터 등록 방식
- app/services/hybrid_analyzer.py 핵심 로직
- 프론트엔드 기존 Chart.js 시각화

✅ **반드시 유지:**
- 기존 기능 100% 동작
- 응답시간 성능 수준 (5초 이내)
- WebSocket 실시간 통신
- 파일 업로드/분석 플로우
- 엑셀 다운로드 기능

## 📝 구현 단계
### Step 1: 백업 및 준비
[구체적인 백업 명령어]

### Step 2: 코드 구현
[상세한 구현 지시사항]

### Step 3: 통합 및 테스트
[테스트 시나리오]

## ✅ 완료 체크리스트
- [ ] 기존 대시보드 정상 로딩 (http://localhost:8002)
- [ ] 파일 업로드/분석 정상 작동
- [ ] 엑셀 다운로드 정상 작동
- [ ] 새 기능 정상 작동
- [ ] 성능 저하 없음 (응답시간 < 5초)
- [ ] 콘솔 에러 없음
- [ ] WebSocket 연결 정상

## 🔧 예상 이슈 & 해결책
[구체적인 문제와 해결 방법]

## 📁 파일 구조 참고
[관련 파일들의 위치와 역할]
```

---

## 🔧 작업 유형별 템플릿

### 1. 새 기능 추가 템플릿

```markdown
# AIRISS - [기능명] 추가 작업 지시서

## 🎯 작업 목표
[기능명]을 AIRISS v4.1에 추가하여 [구체적 비즈니스 가치] 달성

## 📝 구현 단계

### Step 1: 새 모듈 생성
```bash
# 백업
cp app/services/hybrid_analyzer.py app/services/hybrid_analyzer_backup.py

# 새 파일 생성
touch app/services/[모듈명].py
```

### Step 2: 핵심 클래스 구현
```python
# app/services/[모듈명].py 내용
class [클래스명]:
    def __init__(self):
        # 초기화 로직
        pass
    
    def [핵심함수명](self, input_data):
        # 핵심 로직
        return result
```

### Step 3: 기존 시스템과 통합
- **수정 파일**: app/services/hybrid_analyzer.py
- **추가할 import**: `from app.services.[모듈명] import [클래스명]`
- **통합 지점**: `comprehensive_analysis()` 함수 내

### Step 4: API 엔드포인트 추가 (선택사항)
- **새 라우터**: `/api/v2/[기능명]` (기존 API와 분리)
- **main.py 수정**: 새 라우터 등록

### Step 5: 프론트엔드 통합 (선택사항)
- **수정 파일**: app/templates/dashboard.html
- **추가 요소**: 새 차트/위젯
- **JavaScript**: Chart.js 설정 추가

## ✅ 완료 체크리스트
- [ ] 새 모듈 정상 import
- [ ] 기존 분석 결과와 호환성 확인
- [ ] 새 기능 단위 테스트 통과
- [ ] 전체 시스템 통합 테스트 통과
- [ ] 메모리 사용량 4GB 이하 유지
- [ ] 로그에 에러 없음
```

### 2. 버그 수정 템플릿

```markdown
# AIRISS - [버그명] 수정 작업 지시서

## 🐛 버그 상황
- **증상**: [구체적인 에러 현상]
- **발생 조건**: [언제, 어떤 상황에서]
- **영향 범위**: [어떤 기능이 영향받는지]
- **긴급도**: [HIGH/MEDIUM/LOW]

## 🔍 원인 분석
[예상되는 원인과 근거]

## 🛠️ 수정 계획

### Step 1: 현재 상태 확인
```bash
# 에러 로그 확인
tail -f logs/app.log

# 문제 파일 백업
cp [문제파일] [문제파일].backup
```

### Step 2: 수정 구현
- **수정 파일**: [구체적 파일 경로]
- **수정 함수**: [함수명]
- **변경 내용**: [구체적 변경사항]

### Step 3: 테스트
- **테스트 시나리오**: [문제 상황 재현 방법]
- **성공 기준**: [어떻게 확인할지]

## ⚠️ 주의사항
- 기존 동작하는 부분은 절대 변경하지 말 것
- 최소한의 변경으로 문제만 해결할 것
- 변경 전후 동작 비교 테스트 필수

## 🔄 롤백 계획
[문제 발생시 원복 방법]
```

### 3. 성능 최적화 템플릿

```markdown
# AIRISS - [대상] 성능 최적화 작업 지시서

## 📊 현재 성능 현황
- **측정 대상**: [구체적 기능/모듈]
- **현재 성능**: [응답시간, 메모리 사용량 등]
- **목표 성능**: [개선 목표치]
- **병목 지점**: [성능 저하 원인]

## 🚀 최적화 전략

### Step 1: 성능 측정 코드 추가
```python
import time
import psutil

def measure_performance(func):
    def wrapper(*args, **kwargs):
        start_time = time.time()
        start_memory = psutil.Process().memory_info().rss
        
        result = func(*args, **kwargs)
        
        end_time = time.time()
        end_memory = psutil.Process().memory_info().rss
        
        print(f"실행시간: {end_time - start_time:.3f}초")
        print(f"메모리 증가: {(end_memory - start_memory) / 1024 / 1024:.1f}MB")
        
        return result
    return wrapper
```

### Step 2: 최적화 구현
- **캐싱 추가**: [어떤 데이터를 캐싱할지]
- **알고리즘 개선**: [어떤 로직을 개선할지]
- **메모리 최적화**: [메모리 사용량 줄이는 방법]

### Step 3: 성능 검증
- **Before/After 비교**: 최적화 전후 성능 측정
- **부하 테스트**: 대용량 데이터로 테스트
- **장시간 테스트**: 메모리 누수 확인

## 📈 성공 기준
- 응답시간 [X]% 개선
- 메모리 사용량 [Y]MB 이하
- 대용량 파일(1000+ 레코드) 처리 가능
```

### 4. UI/UX 개선 템플릿

```markdown
# AIRISS - [UI 요소] 개선 작업 지시서

## 🎨 개선 목표
[어떤 사용자 경험을 개선할지 구체적으로]

## 🖥️ 현재 UI 상태
- **대상 파일**: app/templates/dashboard.html
- **현재 문제점**: [구체적인 문제]
- **사용자 불편사항**: [실제 사용자 피드백]

## ✨ 개선 계획

### Step 1: HTML 구조 개선
```html
<!-- 기존 코드 백업 후 수정 -->
<div class="[새로운 클래스]">
    <!-- 개선된 구조 -->
</div>
```

### Step 2: CSS 스타일 추가
```css
/* app/static/css/style.css 또는 dashboard.html 내 <style> */
.[새로운클래스] {
    /* 새 스타일 */
}
```

### Step 3: JavaScript 기능 추가
```javascript
// 새로운 인터랙션 로직
function [새함수명]() {
    // 구현
}
```

## 📱 반응형 고려사항
- 모바일/태블릿 호환성
- 다양한 화면 크기 대응
- 터치 인터페이스 지원

## ✅ UI 테스트 체크리스트
- [ ] Chrome 브라우저 정상 표시
- [ ] Firefox 브라우저 정상 표시
- [ ] 모바일 화면 정상 표시
- [ ] 기존 기능 동작 확인
- [ ] 새 기능 사용성 확인
```

### 5. 데이터 처리 개선 템플릿

```markdown
# AIRISS - [데이터 처리] 개선 작업 지시서

## 📊 데이터 처리 현황
- **처리 대상**: [어떤 데이터]
- **현재 방식**: [기존 처리 로직]
- **문제점**: [속도, 정확도, 메모리 등]
- **개선 목표**: [구체적 개선 사항]

## 🔄 개선 방안

### Step 1: 데이터 전처리 개선
```python
def improved_data_preprocessing(data):
    # 개선된 전처리 로직
    # 1. 데이터 검증
    # 2. 결측치 처리
    # 3. 이상치 제거
    # 4. 정규화
    return processed_data
```

### Step 2: 분석 알고리즘 최적화
- **기존 방식**: [현재 알고리즘]
- **개선 방식**: [새 알고리즘]
- **예상 효과**: [성능/정확도 개선]

### Step 3: 결과 후처리 개선
```python
def improved_result_processing(raw_results):
    # 개선된 후처리 로직
    # 1. 결과 검증
    # 2. 포맷팅
    # 3. 추가 인사이트 생성
    return final_results
```

## 📈 성능 벤치마크
- **처리 속도**: 기존 대비 [X]% 개선
- **메모리 효율**: [Y]MB 절약
- **정확도**: [Z]% 향상

## 🧪 테스트 시나리오
1. 소량 데이터 (10개 레코드) 테스트
2. 중간 데이터 (100개 레코드) 테스트
3. 대량 데이터 (1000+ 레코드) 테스트
4. 극한 데이터 (최대 크기) 테스트
```

---

## 🎯 특수 상황별 템플릿

### 긴급 수정 템플릿

```markdown
# 🚨 AIRISS - 긴급 수정 작업 지시서

## 🔥 긴급 상황
- **문제**: [무엇이 안되는지]
- **영향**: [누가, 언제까지 영향받는지]
- **긴급도**: CRITICAL

## ⚡ 빠른 수정 계획
### 즉시 실행 (5분 내)
1. [가장 빠른 임시 조치]
2. [서비스 복구 확인]

### 근본 수정 (30분 내)
1. [원인 제거]
2. [안정성 확보]
3. [재발 방지 조치]

## 🛡️ 안전 장치
- 변경 전 백업 필수
- 단계별 검증 필수
- 롤백 계획 준비
```

### 실험적 기능 템플릿

```markdown
# 🧪 AIRISS - [실험 기능] 프로토타입 작업 지시서

## 🔬 실험 목적
[왜 이 기능을 실험하는지]

## 🎯 가설
[예상되는 결과와 근거]

## 🛠️ 프로토타입 구현
### 별도 브랜치에서 작업
```bash
git checkout -b experiment/[기능명]
```

### 최소 기능 구현
- **핵심 로직만**: 완성도보다 검증에 집중
- **별도 파일**: 기존 코드에 영향 없이
- **간단한 테스트**: 가설 검증 가능한 수준

## 📊 실험 결과 측정
- **성공 기준**: [어떻게 성공을 판단할지]
- **실패 기준**: [언제 실험을 중단할지]
- **데이터 수집**: [어떤 메트릭을 측정할지]

## 🔄 다음 단계
- **성공시**: 정식 기능으로 개발
- **실패시**: 학습 내용 정리 후 종료
```

---

## 💡 프롬프트 작성 가이드라인

### ✅ 좋은 프롬프트 예시
```
"app/services/bias_detector.py 파일을 생성하여 성별, 연령대별 점수 편향을 탐지하는 BiasDetector 클래스를 구현하세요. 
detect_bias(df) 함수는 DataFrame을 입력받아 4/5 규칙을 적용하여 편향 여부를 판단하고, 
{'is_biased': True/False, 'affected_groups': [...], 'recommendation': '...'} 형태로 결과를 반환해야 합니다."
```

### ❌ 나쁜 프롬프트 예시
```
"편향 탐지 기능을 추가해주세요."
```

### 🎯 핵심 원칙
1. **구체성**: 파일명, 함수명, 입출력 형태까지 명시
2. **단계화**: 큰 작업을 작은 단위로 분해
3. **예시 제공**: 코드 스니펫, 예상 결과 포함
4. **검증 가능**: 완료 여부를 객관적으로 확인 가능

---

**"명확한 지시가 완벽한 구현을 만듭니다!"** 🎯